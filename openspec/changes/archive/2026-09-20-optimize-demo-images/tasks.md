# Tasks

> 為了 GitHub Pages 上的展示所做的優化（見 proposal）。任務群組 1 對應 commit `35923bc`，群組 2 對應更正註解與寫文件的 commit。

## 1. 圖片縮到顯示尺寸的兩倍

- [x] 1.1 先量再改：列出每個素材資料夾的圖片寬度與畫面上的顯示寬度，並以 curl 量 GitHub Pages 未快取時的等待時間；驗證：商品圖最多是顯示尺寸的 7.8 倍、猜你想搜 5.4 倍、banner 類都在 2.5 倍以內；首頁 157 張圖合計 6,842 KB；未快取的檔案約 0.45 秒才開始傳
- [x] 1.2 測試先行：詳情頁的主圖要用大圖（對舊實作紅：它顯示的是卡片的圖）；app 層的 spec 檢查每件商品的卡片圖是 WebP、尺寸正好是「出現過的最大卡片 × 2」（原圖比較小時為原圖大小）、詳情頁的圖是同一張圖的大版本且在 880px 以內、猜你想搜在 372px 以內且版位資料記錄的尺寸與檔案相同；驗證：對舊的素材與資料全部紅燈，理由各自正確（114 張圖過大、卡片圖不在 `card/`、猜你想搜 1000px）
- [x] 1.3 `tools/import-assets.mjs` 產生兩個 WebP 版本（`card/` 與詳情頁用的大圖）、猜你想搜縮到 372px、縮圖的資料夾每次整個重新產生；`sharp` 為根目錄的開發依賴；驗證：重跑兩次，整個資料夾的雜湊相同。**實作中抓到的錯**：工具裡辨識商品 id 的正規表示式少了一個反斜線（`\d` 變成 `d`），「依商品出現過的最大卡片」完全沒有生效、也沒有任何錯誤訊息；是 1.2 的「尺寸正好是兩倍」那條測試抓到的（4 件跨區的商品是 416 / 280px，應為 450px）
- [x] 1.4 `tools/gen-fixtures.mjs`：`imageUrl` 指向卡片圖、`images` 是大圖、略過 `card/` 資料夾、卡片圖不存在就失敗；詳情頁改顯示 `images` 的第一張；猜你想搜的版位資料改記 372 × 372；驗證：1.2 的測試全部通過，`pnpm verify:fixtures` 通過
- [x] 1.5 規格情境逐條以實際資料核對：只在降價好貨、原圖 1000px 的商品 → 卡片圖 256px（`TP00003980002596`）；同時在限時搶購與「你可能會喜歡」、原圖 640px → 450px（3 件）；原圖 640px 的 WebP → 詳情頁的圖與原檔逐位元組相同。「原圖比需要的小」在現有資料裡一件都沒有，改用一張 320px 的樣本圖跑同樣的縮圖設定，結果仍是 320px
- [x] 1.6 瀏覽器（1440px）：首頁同樣 157 張圖 6,842 → 3,647 KB（−47%），其中商品圖與猜你想搜 4,211 → 1,014 KB（−76%），banner 不變；每張已載入的卡片圖都是顯示寬度的 1.99–3.21 倍，沒有低於 1 倍的；原圖 1000px 的商品，詳情頁主圖 880 × 880、103 KB（原本 205 KB）；完整關卡（10 個專案 lint / test / typecheck、build、E2E 3 / 3、format、兩個 verify）全綠
- [x] 1.7 部署後在正式站量：同樣的 157 張圖約 3,646 KB，與本機一致；CDN 上的猜你想搜已是新檔。**觀察到的現象**：檔名沒變的圖（猜你想搜）在 10 分鐘內造訪過的瀏覽器裡會先顯示快取的舊檔（GitHub Pages 的 `max-age=600`），之後自動更新

## 2. 更正與文件

- [x] 2.1 更正：commit `35923bc` 與程式註解把「詳情頁已經夠小的 WebP 原檔照搬」的理由寫成「重新壓縮不會變小」。對照兩次匯入的數字，重新壓縮其實會變小（momo 店取 318 → 198 KB、限時搶購 743 → 716 KB、你可能會喜歡 1,528 → 1,471 KB）；真正的理由是畫質（詳情頁一次只載入一張）。註解已改；commit 不改寫，由這次的 commit 與 `docs/agent-workflow.md` 記錄
- [x] 2.2 文件：README、`docs/architecture.md` §9、`docs/agent-workflow.md`、`CLAUDE.md`、Phase 1 與 Phase 2 筆記，都寫明這是為了 GitHub Pages 上的展示所做的優化；驗證：`pnpm format:check`、`openspec validate --all --strict` 通過
