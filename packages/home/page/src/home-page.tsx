/** Walking-skeleton placeholder. The config-driven sections replace the body. */
export function HomePage() {
  return (
    <section>
      <h1 className="text-ec-title font-bold text-ink">首頁</h1>
      <p className="mt-2 text-ec-base text-ink-muted">首頁區塊建置中。</p>
    </section>
  );
}
