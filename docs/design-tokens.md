# 設計 Token

顏色、字級、圓角、陰影都不寫死在元件裡，而是走兩層 token。來源檔在 [`packages/shared/ui/src/styles/`](../packages/shared/ui/src/styles)。

## 兩層結構

| 層            | 檔案                   | 回答的問題                                                                    | 誰可以用                           |
| ------------- | ---------------------- | ----------------------------------------------------------------------------- | ---------------------------------- |
| **Primitive** | `tokens.primitive.css` | 我們**有哪些**顏色 —— 純調色盤，名稱不帶用途（`--momo-magenta-600`）          | 只有 semantic 層。元件**不可以**用 |
| **Semantic**  | `tokens.semantic.css`  | 這個顏色**用在哪裡**（`--color-brand`、`--color-price`、`--color-ink-muted`） | 元件唯一能用的一層                 |

為什麼要兩層：

- **品牌改色**只改 primitive 的一行，所有指向它的 semantic token 一起變。
- **換主題**（深色模式、活動檔期換色）只覆寫 semantic 層，元件完全不用動。
- 同一個色碼可以有兩種用途而不互相綁死：`--color-brand` 與 `--color-action-primary` 現在都指向 `--momo-magenta-600`，但日後「直接購買」要換色時不會連帶改到 logo。

### 怎麼保證元件不會繞過 token

靠結構，不靠自律：

1. **Primitive 刻意不放在 `@theme` 裡**，只是 `:root` 上的一般 CSS 變數，所以 Tailwind 不會為它們產生 utility —— 根本沒有 `text-momo-magenta-600` 可以用。
2. **Tailwind 預設的色盤與字級被關掉**（`--color-*: initial; --text-*: initial;`），所以 `text-pink-600`、`bg-white`、`text-sm` 在這個專案裡不存在。
3. 已用建置產物驗證：產出的 CSS 含有 `.text-brand`、`.bg-footer`、`.max-w-shop` 等 semantic utility，`--color-brand` 的值是 `var(--momo-magenta-600)`，且不含 `--color-pink-600`、`--color-neutral-900`、`--text-sm`。

仍然擋不住的是 Tailwind 的任意值語法（`text-[#d62872]`）。目前靠 review；要工具化的話可以加一條 lint 規則禁止 className 裡出現 `[#`。

## 數值怎麼來的

**不是看截圖估的。** 以瀏覽器開啟真實網站（桌機版面、1440px 寬），用 `getComputedStyle` 讀出元素實際套用的樣式，時間為 2026-09-18。來源頁面：首頁 `/main/Main.jsp` 與一個商品詳情頁 `/product/15642257`。

過程中得知的事實：

- 真實網站以 **Next.js + Tailwind CSS** 建置（頁面有 `next-route-announcer`；class 是 Tailwind utility）。字體是它建置當時 Tailwind 的 sans 堆疊（`ui-sans-serif, system-ui, sans-serif, …`），基礎字級 16px / 行高 24px。Tailwind 4.3 已經改了自己的預設堆疊，所以本專案把量到的值釘成 token（`--font-sans`），而不是繼承工具的預設；已在 dev server 上確認 `body` 的 `font-family` 與真站相同。
- 首頁區塊是**虛擬化渲染**的：只有捲到視窗內才掛上 DOM。
- 字級是一套奇數階梯，真站自己叫它 `ec-*`（`text-ec-sm` = 13px、`text-ec-xl` = 19px、`text-ec-2xl` = 21px）。本專案沿用這個命名以便對照，並補齊沒有名字的階。
- 品牌色在真站的 token 名稱是 `mo` / `momo-pink`；深色文字是 `momo-neutralStrong`。
- 頂部列是 `position: fixed`（不是 sticky）、`z-index: 200`。

### 顏色

| Semantic token         | 值              | 在真站的哪裡量到                                                                   |
| ---------------------- | --------------- | ---------------------------------------------------------------------------------- |
| `brand`                | `#d62872`       | 分類列作用中項目的文字與 3px 底線、熱搜關鍵字、「降價好貨」的價格                  |
| `action-primary`       | `#d62872`       | 詳情頁「直接購買」按鈕背景                                                         |
| `action-cart`          | `#1c6fbc`       | 詳情頁「放入購物車」按鈕背景                                                       |
| `action-neutral`       | `#cccccc`       | 詳情頁「加入追蹤」按鈕背景                                                         |
| `on-action`            | `#ffffff`       | 三顆按鈕的文字、搜尋按鈕文字、footer 連結                                          |
| `price`                | `#db2777`       | 店取、暢銷榜、moPro、「你可能會喜歡」的價格                                        |
| `sale`                 | `#dd2222`       | 限時搶購的價格、「限搶價」標籤、促銷文字                                           |
| `countdown`            | `#ff4c76`       | 限時搶購倒數數字的底色（28×28、圓角 4px、白字 16px 粗體）；Step 11 複核為不透明    |
| `ink-strong`           | `#222222`       | 限時搶購標題列文字                                                                 |
| `ink-emphasis`         | `#262626`       | 詳情頁的說明文字（15px / 22.5px、weight 500）                                      |
| `ink`                  | `#404040`       | 商品名稱、分類列項目、頂部列連結、詳情頁標題與欄位標籤                             |
| `ink-label`            | `#888888`       | 詳情頁「促銷價」標籤、劃線價                                                       |
| `ink-muted`            | `#999999`       | 商品卡的劃線原價、詳情頁的品號                                                     |
| `line`                 | `#e5e5e5`       | 直式商品卡（降價好貨）框線                                                         |
| `line-card`            | `#d9d9d9`       | 橫式商品卡（momo 店取、今日暢銷榜）框線；沒有陰影                                  |
| `line-card-hover`      | `#d1d5db`       | 橫式商品卡 hover 時的框線（真站樣式表：`.hover\:mu-border-gray-300:hover`）        |
| `line-soft`            | `#f0f0f0`       | 限時搶購商品卡框線                                                                 |
| `line-strong`          | `#cccccc`       | 頂部列的底線                                                                       |
| `control`              | `#515151`       | 搜尋框的 2px 邊框與搜尋按鈕背景                                                    |
| `surface-muted`        | `#f2f2f2`       | 頂部列背景；首頁的頁面底色（白色區帶底下的灰）                                     |
| `footer`               | `#09355d`       | footer 背景                                                                        |
| `footer-accent`        | `#53d6df`       | footer 欄位標題、防詐騙提醒的文字                                                  |
| `footer-accent-line`   | `#a9eaef`       | 防詐騙提醒的 3px 框線                                                              |
| `ink-title`            | `#1f2937`       | 展開的分類面板標題「選擇分類」                                                     |
| `ink-icon`             | `#4b5563`       | 分類列展開按鈕的箭頭                                                               |
| `surface-control`      | `#fafafa`       | 分類列展開按鈕的底色（36 × 40px）                                                  |
| `category-sky`         | `#e1eff6` @ 50% | 分類面板第 1 列的膠囊（限時搶購 … 電腦/組件）                                      |
| `category-lavender`    | `#e6e6f9` @ 50% | 第 2 列（3C週邊 … 個人清潔）                                                       |
| `category-rose`        | `#f2d7de` @ 40% | 第 3 列（日用/紙品 … 戶外）                                                        |
| `category-mint`        | `#e2f8e2` @ 50% | 第 4 列（車類 … 生活超市）                                                         |
| `category-peach`       | `#ffe9cc` @ 50% | 第 5 列（跨境好物 … 品牌旗艦館）                                                   |
| `carousel-arrow`       | `#000000` @ 30% | 輪播的上一頁 / 下一頁按鈕底色（降價好貨）                                          |
| `carousel-arrow-hover` | `#000000` @ 40% | 同上，hover（從真站的 class `hover:mu-bg-opacity-40` 讀到）                        |
| `carousel-dot`         | `#ededed`       | 輪播下方不是目前頁的圓點；目前頁用 `brand`                                         |
| `badge`                | `#f73f64`       | 今日暢銷榜標題旁的「即時更新」—— **取樣自目標截圖**，真站的標題列是一張圖          |
| `surface-sale`         | `#ffd9e5`       | 限時搶購標題列的底色 —— **取樣自目標截圖**，真站這一列是一張圖（見「沒有量到的」） |
| `surface-stock`        | `#fdf2f2`       | 限時搶購「最後 N 組」的底色（圓角 10px）                                           |
| `surface-caption`      | `#fce9e9`       | 猜你想搜圖磚下方說明列的漸層終點（`linear-gradient(#fff 5%, #fce9e9)`）            |

詳情頁的價格量到的是 `#d62672`，與品牌色 `#d62872` 只差一個色階，視為同一個顏色（`brand`），不另立 token。

分類膠囊的底色在真站是**半透明**的（`rgba(225, 239, 246, 0.5)` 這種寫法）疊在白色面板上。token 保留這個結構：primitive 存不透明的色碼，semantic 用 `color-mix(in srgb, <primitive> 50%, transparent)` 加上透明度，而不是另外算出一個混合後的色碼 —— 面板底色日後改變時，膠囊的視覺關係仍然正確。在 dev server 上量到的結果與真站相同（`color(srgb 0.88 0.94 0.96 / 0.5)`）。輪播箭頭的黑色 30% 也是同樣的做法。

**劃線原價只用一個 token。** 真站依區塊用了三種幾乎一樣的灰：降價好貨 `#999999`、橫式商品卡 `#9ca3af`、推薦區 `#b3b3b3`。同一個角色在設計系統裡應該只有一個值，所以 `PriceTag` 一律用 `ink-muted`（`#999999`）。原本為推薦區準備的 `ink-subtle`（`#b3b3b3`）因此沒有使用者，已在 Step 9 移除（連同它的 primitive）。

**版位資料裡的顏色不是 token。** 今日暢銷榜的粉色區帶（`#f6e8eb`）寫在 `home-layout.ts` 那一筆設定的 `background`，不在 token 表裡。真站的每個區塊容器都會收到一個 inline 的 `background-color`（其他區塊是白色）：它是 CMS 逐區塊、隨檔期給的內容，和 banner 圖片同一類。token 描述設計系統裡固定的角色；放進 token 的話，行銷每換一次檔期顏色就要改程式、發版。代價是資料可以送來任何顏色，設計系統管不到。

**量到了但沒有使用者的 token 不留。** Step 4 一口氣量了兩個頁面上看得到的顏色，其中有些對應的東西後來決定不做：`ink-meta`（`#454545`，推薦區的「總銷量」—— 資料模型沒有這個欄位）與 `breadcrumb-root`（`#bb7711`，詳情頁麵包屑的「Home」—— 不在需求內）已在 Step 10 移除。token 表應該只描述畫面上真的有的東西；數值留在這段文字裡，之後要做時不用重量。`countdown` 與 `line-soft` 屬於限時搶購，Step 11 做了之後它們有了使用者，留下。

### 字級

| Token         | 大小 / 行高 | 用在哪裡                                                                            |
| ------------- | ----------- | ----------------------------------------------------------------------------------- |
| `ec-2xs`      | 11 / 16     | 「限搶價」、銷量                                                                    |
| `ec-sm`       | 13 / 18     | 頂部列、熱搜關鍵字、劃線原價、剩餘組數、欄位標籤、品號                              |
| `ec-base`     | 15 / 20     | 商品名稱、促銷文字、搜尋按鈕、詳情頁說明                                            |
| `ec-md`       | 16 / 24     | 動作按鈕、搜尋輸入框                                                                |
| `ec-lg`       | 17 / 25.5   | 分類列項目（weight 600）                                                            |
| `ec-xl`       | 19 / 25     | 詳情頁商品標題（weight 700）、推薦區價格、footer 標題、倒數標籤                     |
| `ec-2xl`      | 21 / 26     | 商品列的價格                                                                        |
| `ec-3xl`      | 23 / 28.75  | 限時搶購價格                                                                        |
| `ec-4xl`      | 25 / 37.5   | 詳情頁價格                                                                          |
| `panel-title` | 18 / 28     | 「選擇分類」（weight 700）—— 全站唯一的偶數字級，所以依用途命名而不放進 `ec-*` 階梯 |
| `ec-title`    | 24 / 32     | 區塊標題 —— **未實測，見下**                                                        |

### 元件尺寸與框線

| 元件                 | 量到的值                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| -------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 頂部列               | 高 40px（含底線 41px）、`fixed`、背景 `surface-muted`、底線 1px `line-strong`、文字 13px                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| 搜尋框               | 高 36px、2px `control` 邊框、左側膠囊圓角；按鈕背景 `control`、白字 15px / 600、右側膠囊圓角                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| 分類列               | 項目 17px / 600、高 37px、間距 16px；作用中：`brand` 文字 + 3px `brand` 底線                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| 分類列的展開按鈕     | 36 × 40px、背景 `surface-control`、箭頭 `ink-icon`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| 「選擇分類」面板     | `absolute`、接在分類列正下方、寬 1220px、白底、下方圓角 12px、陰影 `0 2px 8px rgb(0 0 0 / 0.08)`；內部 9 欄 × 124px 的 grid、間距 10px、padding 12px；共 40 個分類（9 + 9 + 9 + 9 + 4）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| 分類膠囊             | 124 × 44px、圓角 22px、padding 12px 16px、15px / 20px `ink-strong`、置中、過長以 `…` 截斷；作用中（首頁）：白底 + 1px `brand` 框線 + `brand` 文字                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| 降價好貨商品卡       | 1px `line`、圓角 8px、`shadow-card`；名稱 15px `ink`；`$` 13px / 500；價格 21px / 700 `brand`；原價 13px `ink-muted` 劃線                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| 降價好貨商品卡的尺寸 | 卡片 130.7 × 238.3、圖 128 × 128（`object-fit: cover`）、文字區左右內距 10px、圖與名稱間距 8px；名稱兩行（15 / 20）；卡片間距約 10px，1220px 內可見約 8.45 張（最後一張露出一部分，暗示後面還有）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| 橫式商品卡           | momo 店取與今日暢銷榜共用（真站上兩者的 class 字串完全相同）：335 × 174、1px `line-card`（`#d9d9d9`）、圓角 8px、**沒有陰影**、padding 16px、圖 140 × 140 在左、圖文間距 10px；促銷文字 13 / 18 `sale` 單行；名稱 15 / 20 兩行；價格 21px / 700 `price`，原價在**同一列**右側；卡片間距 10px（每張間隔 345.4px），可見約 3.45 張                                                                                                                                                                                                                                                                                                                                                                                                             |
| 輪播箭頭             | 32 × 56px、貼齊容器左右邊緣、垂直置中；朝內的一側圓角 8px；白色箭頭；停用時 `opacity: 0.25`；`aria-label` 為「上一頁」「下一頁」                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| 輪播圓點             | 兩種位置。**下方**（商品列）：4 × 4px 圓點、間距 4px；目前頁是 12 × 4px 的膠囊、`brand` 色；寬度變化有 300ms 過場；位於商品列下方 12px、置中。**疊在圖上**（主要活動）：3 × 3px、目前頁 8 × 3px、間距 3px，距圖的下緣 12px（真站的 class：`absolute inset-x-0 bottom-0` 裡一列 `my-3`）                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| 首頁的頁面與區塊     | 頁面底色 `surface-muted`（`#f2f2f2`）滿版；每個區塊是 1220px 寬的白色區帶，內容左右 padding 16px（內容寬 1188px）；有標題的區塊：標題列 70px，下方 16px 才是內容，底部的留白由輪播圓點那一列提供（12 + 4 + 12px）—— 降價好貨區帶 352px、momo 店取與今日暢銷榜 294px、猜你想搜 348px；今日暢銷榜的區帶底色 `#f6e8eb` 來自版位資料；旗艦名店與超大牌例外，圖片貼齊 1220px、沒有間距（3 × 406、4 × 305）                                                                                                                                                                                                                                                                                                                                        |
| 主要活動（hero）     | 區帶高 476.8px；banner 327 × 444.8、間距 12px、一次約 2.57 張；右側「今日大牌」面板寬 316.4px，2 × 2 共四格，每格 158.2px 寬                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| 各輪播一次可見的張數 | 官方優惠圖示 148.5 × 148.5、8 張、無間距；品牌折扣與 moPro 218.9 × 364.8、約 5.43 張、無間距；信用卡加碼 250 × 125、約 4.75 張、無間距；猜你想搜 186 × 234、間距 10px、約 6.11 張                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| 猜你想搜圖磚         | 圖 186 × 186；說明列高 48px、白到 `surface-caption` 的漸層、下方圓角 4px；文字 15 / 20、weight 700、`ink`、置中                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| 限時搶購商品卡       | 229.6 × 386、圖 208 × 208；1px `line-soft`、圓角 8px、`shadow-card-raised`（hover 時 `shadow-card-hover`）、padding 10px。由上到下：圖 → 4px → 促銷 15px / 700 `sale`、行高 19.95 → 2px → 名稱 15px、行高 21.45、兩行（最少 42px）→ 2px → 「限搶價」與 `$` 11px / 700 `sale`、價格 23px / 700 `sale`（行高 28.75）→ 2px → 劃線原價 13 / 18 → 6px → 「最後 N 組」13px / 700 `ink`、行高 1.45、padding 2px 8px、底色 `surface-stock`、圓角 10px（`radius-chip`）→ 8px。**「搶」**：貼齊卡片右下角的標籤，82 × 29、`#dc2626`（視為 `sale`）、左上圓角 13px（`radius-tab`）、padding 5px 30px，裡面是白色的「搶」字形。每頁 5 欄 × 2 列，欄距 239px、列距 396px。本專案（2026-09-20 調整後）卡片 385.4px、列距 395.4px，只有劃線原價比真站高 2px |
| 推薦商品卡           | 無框線、圓角 4px；價格 19px / 700 `price`；原價 13px（真站 `#b3b3b3`，本專案統一用 `ink-muted`）；銷量 11px `#454545`（沒有做：資料模型沒有銷量）；5 欄、每格約 224px、欄距 16px                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| 詳情頁的版面         | 1220px 容器、左右 padding 16px；左欄主圖 440 × 440（`object-contain`）、欄距 32px；右欄：促銷文字 15px `sale` → 標題 19px / 700 `ink` → 條列說明 15px / 500 `ink-emphasis` 與右側品號 13px `ink-muted` → 上下 1px `line` 的價格區（「促銷價」13px `ink-label` + 價格 25px / 700 `brand` + 「元」；市售價 13px 劃線）→ 三顆按鈕，間距 12px。主圖寬度、欄距與按鈕間距取自目標截圖，其餘為實測值                                                                                                                                                                                                                                                                                                                                                |
| 詳情頁動作按鈕       | 160 × 40px、**直角（0px）**、16px / 600 白字                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| Footer               | 背景 `footer`、內容寬 1220px、上方 padding 50px；標題 19px / 700 `footer-accent`；連結 13px 白色；防詐騙提醒 3px `footer-accent-line` 框線、圓角 8px、padding 14px                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| 內容容器             | 1220px（`max-w-shop`）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |

## 沒有量到的

| 項目                                                 | 原因                                                                                                                                                                                                                 | 目前的做法                                                                                                                                                                         |
| ---------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 區塊標題（「降價好貨」「今日暢銷榜」…）的字級與雙色  | **標題在真站上不是文字，是一張 1220×70 的背景圖**（例：moPro 的標題是 `bt_7_703_02_e7.png`），掛在一條沒有文字的連結上。先前寫的「推測位於跨來源的 iframe 內」是錯的：那只是沒找到文字時的猜測，沒有驗證就寫進了文件 | 提供的素材裡沒有這些標題圖，所以本專案用文字呈現標題；字級依截圖估計為 24px，前半淺灰、後半深色。token 標註為「估計值」                                                            |
| 「看更多」按鈕的尺寸、框線與文字顏色                 | 第一次：真站的「你可能會喜歡」要捲動到才會掛載，量測時預覽面板在背景。第二次（2026-09-20）：這一區在真站已換成另一種版型，頁面上找不到「看更多」                                                                     | 依目標截圖估計：約 184 × 38px、膠囊形、1px `line` 框線、15px `ink-label` 文字、右側向下箭頭                                                                                        |
| hover / focus 狀態的顏色                             | 只量了靜態樣式；2026-09-20 從真站的樣式表讀到兩個商品卡的 hover（見上方 `line-card-hover` 與「限時搶購商品卡」），其他元件的 hover 與所有 focus 狀態仍未量                                                           | 已量到的兩個照做；其餘沿用各元件的預設                                                                                                                                             |
| 限時搶購標題列的粉色底、火焰圖示與標題字樣           | **Step 11 查證：整條 1220×70 的標題列是一張圖**（`bt_7_713_01_e9.png`），和其他區塊標題一樣，沒有可以讀的計算樣式                                                                                                    | 粉色底 `surface-sale`（`#ffd9e5`）從目標截圖 `11-flash-sale.png` 取樣；火焰用 Heroicons 的 `fire`（MIT）、標題用文字重現。「倒數」兩字（19px `ink`）與數字方塊是真的 DOM，為實測值 |
| footer 背景、footer 標題、防詐騙提醒框線的**透明度** | 量這幾個值時用的轉換函式會把 alpha 丟掉（後來量分類膠囊時才發現）。想重新核對時瀏覽器面板處於隱藏狀態，虛擬化的區塊沒有掛載，讀不到。同一批的倒數數字底色已在 Step 11 以原始字串複核：`rgb(255, 76, 118)`，不透明    | 從截圖看 footer 是實色，影響很小；token 先視為不透明                                                                                                                               |
| 主 header 右側的三張活動小圖                         | 素材資料夾裡沒有這三張圖                                                                                                                                                                                             | 不做，列入 Known Gaps                                                                                                                                                              |

估計值在 `tokens.semantic.css` 裡都有註解標明，之後量到就直接更新 token，元件不用改。
