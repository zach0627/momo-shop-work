# Human ↔ Agent 協作紀錄

這個專案由一位工程師（Human）與 Claude Code（Agent）協作完成。這份文件記錄**怎麼分工、Human 在哪裡糾正了 Agent、Agent 在哪裡出錯、以及從中得到的做法**。它與 git history 一起，是「AI / Agent Supervision」的第一手紀錄。

逐步計畫與完成紀錄在 [`MoMO面試/Phase 2 — Implementation.md`](./MoMO面試/Phase%202%20—%20Implementation.md)。

## 1. 工作協議

先設計、後實作；設計文件是 source of truth。

每一個實作步驟固定走這個迴圈，Agent 不跳步、不偷做下一步：

1. **實作** — 有邏輯的部分先寫測試 → 確認紅燈，且是因為**正確的理由**紅 → 最小實作 → 綠燈。
2. **驗證** — 跑該步的驗證指令（驗證關卡不使用快取），貼出實際輸出。失敗照實說。
3. **回報** — 變更清單、驗證結果、有沒有偏離設計以及原因。
4. **Review** — Human 看過才 commit。
5. **Commit** — Conventional Commits，帶 `Co-Authored-By`。每個 commit 後 `main` 都是綠的。

分工原則：**決定權在 Human 的事，Agent 提出建議與理由後停下來等**（架構取捨、要不要公開某個檔案、專案放在哪個磁碟）；**可逆、且在已同意範圍內的事，Agent 直接做並回報**。

## 2. Human 糾正 Agent 的紀錄

設計階段 Human 共糾正 Agent 四次。每一次都讓設計變得更好，值得記下來。

| #   | Agent 原本的提案                                                                   | Human 的糾正                                                                           | 結果                                                                                                                                                                                                  |
| --- | ---------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | 詳情頁的三顆按鈕加上互動：放入購物車讓徽章 +1、加入追蹤可切換、直接購買跳提示      | 需求筆記明寫詳情頁是展示用、不可擅自往下做。Agent 讀了卻仍然提案超出範圍的東西         | 三顆按鈕只渲染、不綁行為。連帶移除 `cart/data-access` 與 Zustand —— 盤點後全域 client state 為 0（[ADR-0003](./adr/0003-server-state-only-no-global-store.md)）                                       |
| 2   | 首頁 13 個區塊全部放進同一個 `home/feature` lib                                    | 「首頁明明有這麼多區塊，為何不做 features？」                                          | Agent 重新檢視後發現原案確實有缺陷：「你可能會喜歡」在詳情頁也會出現，埋在 `home` 裡 `goods` 就拿不到。改為依「有沒有自己的邏輯 / 資料 / 重用性」切分，並新增 `type:page` 作為唯一能組合 feature 的層 |
| 3   | 把看起來通用的元件都放進 `shared/ui`（含只有一個使用者的「看更多」按鈕、倒數計時） | 「被兩個以上 feature 共用才該放 shared；只有自己用的應該留在自己的 feature 底下」      | 訂出 **Rule of Two** 與 lib 內部的 `ui/`、`model/` 私有資料夾。`shared/ui` 從 10 個元件瘦身到 6 個，同一條規則也套用到 `shared/util`                                                                  |
| 4   | （實作兩步之後）Agent 認為架構已定案                                               | 「我們大量寫在 lib 上，以後擴充購物車、結帳、品牌頁、刷卡時，libs 不會變得很肥大嗎？」 | Agent 承認三個真實弱點與一個事實：以目前規模，這個結構是偏重的。產出 lib 粒度準則、拆分觸發條件與 [ADR-0006](./adr/0006-domain-dependency-map.md)，並把代價照實寫進 README                            |

共同的模式：**Agent 擅長把一個方向做完整，但不擅長質疑方向本身。** 四次糾正都是 Human 從「這樣以後會怎樣」的角度提問，而不是指出某一行寫錯。

## 3. Agent 的自我審查（設計 v1 → v2）

Human 要求 Agent 在定案前重新審查自己的設計。Agent 找到 8 個問題，其中影響最大的三個：

1. `ProductCard` 沒講清楚吃什麼型別 → 直接吃 `Product` 就會變成 `ui → data-access`，違反自己訂的分層。改為 ui 宣告最小形狀、靠 structural typing 傳入。
2. hooks 直接 import repository singleton → 「只換一個實作檔」其實做不到，測試也只能吃 55 筆真 fixture。改為 Context 注入。
3. 交付順序是水平分層（util → ui → data → …）→ 時間用完時可能連詳情頁都還沒出現。改為 walking skeleton 先行，不可砍的先做。

完整 8 點見[設計筆記](./MoMO面試/Phase%201%20—%20%20Design%20and%20Planning.md)開頭。

## 4. Agent 出錯與事故紀錄

| 事件                  | 發生了什麼                                                                                                                                                                                                                                                                                                                            | 怎麼發現                                                           | 之後的做法                                                                                                                                                      |
| --------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 並行編輯              | Agent 寫入一份筆記時，Human 同時在 Obsidian 編輯同一個資料夾；另一份筆記的內容被覆蓋。Agent 發現異常後只口頭提了一句，就繼續往該檔案追加內容                                                                                                                                                                                          | Human 發現筆記內容不對                                             | 用 vault 的 git 備份逐位元組還原並以 hash 驗證。此後寫入共用檔案前先確認檔案仍是預期狀態（hash / 關鍵字），不符就中止；**發現異常要先停下來確認，而不是繼續做** |
| tags 靜默失效         | PowerShell 把 `--tags=type:util,scope:shared` 的逗號當成陣列分隔，tags 變成單一字串 `"type:util scope:shared"`。boundary 規則對這個 lib 完全失效，而且不會報錯                                                                                                                                                                        | Agent 產生第一個 lib 後檢視產出才發現                              | 參數加引號；**不信任 generator 的輸出，逐一核對全部 10 個 lib 的 tags**；再用負向驗證確認規則真的會擋                                                           |
| 誤判為通過的風險      | 全量驗證時 `shop:test` 失敗。原因是機器忙碌時 Vitest worker 啟動逾時，不是測試沒過                                                                                                                                                                                                                                                    | 讀錯誤訊息而不是只看 exit code                                     | 單獨重跑確認為綠才算通過，並在回報中說明；之後驗證一律 `--parallel=1`                                                                                           |
| 即將公開個資          | 17 張截圖中有 2 張是登入狀態，頂部列有遮罩後的帳號姓名                                                                                                                                                                                                                                                                                | Agent 在推上 public repo 前把每張圖都看過                          | 只在 repo 的複本上遮蓋，原圖不動；由 Human 決定是否保留遮蓋。原則：**不可逆的動作（公開）選保守的一邊，可逆的那一邊留給 Human 決定**                            |
| 假綠燈：lockfile 過期 | commit 進去的 `pnpm-lock.yaml` 缺了 8 個 lib 的 importer 條目。本機因為 `node_modules` 已存在而一切正常，但全新 clone 後 `pnpm install --frozen-lockfile`（CI 的預設）會直接失敗。成因：pnpm 12.4.2 寫 lockfile 時不會為「沒有宣告依賴的新 workspace 專案」補條目，檢查時卻要求每個專案都有 —— 它自己產生的 lockfile 通不過自己的檢查 | 在另一顆磁碟建立沒有 `node_modules` 的測試複本時，第一次安裝就失敗 | 補齊條目並在全新環境以 `--frozen-lockfile` 驗證通過。**「在我的機器上是綠的」不等於「可重現」**：驗證要在乾淨環境跑一次。新增 lib 後要確認 lockfile 有它的條目  |
| 診斷時的錯誤          | 追查上一項時，Agent 用來列出 lockfile 條目的 regex 寫錯（漏掉 `path: {}` 這種格式），因而誤判「只有 1 個條目」，並據此在測試複本上插入了重複的 key                                                                                                                                                                                    | pnpm 回報 duplicate key                                            | 直接讀原文確認，而不是依賴自己寫的 regex 推測。錯誤的修改只發生在測試複本上，沒有碰到基準 —— 這正是複本存在的理由                                               |

## 5. 與原計畫的偏離

| 步驟     | 偏離                                                              | 原因                                                                                                                                                                                                                                      |
| -------- | ----------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Scaffold | 沒有使用 Nx 的 `react-monorepo` preset                            | Nx 23 的 preset 改為下載官方範本並忽略 flags；下載到的是一個完整的示範電商專案（含 `feature-products`、`feature-product-detail`）。領域與本題相同，直接使用會讓審閱者看到的是 Nx 的架構而不是我們的設計。改為「空 workspace + generator」 |
| Scaffold | 移除範本附帶的約 70 個檔案                                        | 多種 AI 工具的設定、一份會自動啟用第三方 plugin 市集的 `.claude/settings.json`、一份使用 npm + Nx Cloud 而必然失敗的 CI。原則：repo 裡的每個檔案都要是我們說得清楚的                                                                      |
| Scaffold | 採用 pnpm workspaces + TS project references，而非 tsconfig paths | 範本模式下 `--workspaces=false` 無效。這是 Nx 目前的標準做法，`@momo/*` 由各 lib 的 package name 提供，設計不受影響                                                                                                                       |
| Scaffold | pnpm `allowBuilds` 只核准 `nx` 與 `@swc/core`                     | pnpm 12 預設不執行依賴的安裝腳本。用明確的 allow-list 而不是 `--all`，之後有新套件想執行腳本時仍會被擋下來判斷                                                                                                                            |
| Libs     | 3 個 feature lib 的 placeholder 延到真正需要時才加                | 目前用不到                                                                                                                                                                                                                                |
| Libs     | 空的 lib 設了 `passWithNoTests`                                   | Vitest 找不到測試檔會判定失敗。該 lib 出現第一個 spec 時要移除                                                                                                                                                                            |

## 6. 環境備註

專案位於 5400 轉的傳統硬碟（系統碟才是 NVMe SSD），且防毒即時掃描開啟。`node_modules` 有數萬個小檔，導致每個 Nx / Vitest 指令光讀檔就 30 秒以上 —— 實測一個 0.5 秒的測試總耗時 34 秒（import 16 秒 + jsdom 環境 16 秒）。**與程式碼無關。**

決定：專案維持在原磁碟。因應方式：

```bash
NX_DAEMON=false NX_ISOLATE_PLUGINS=false pnpm nx run-many -t lint test typecheck --parallel=1
```

- `NX_ISOLATE_PLUGINS=false`：否則 plugin worker 子行程無法在 30 秒內連線而載入失敗。
- `--parallel=1`：否則 Vitest worker 啟動逾時。
- 日常開發使用 Nx 快取；只有驗證關卡才加 `--skip-nx-cache`。

在一般的 SSD 環境不需要這些參數。
