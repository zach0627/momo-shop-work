# Human ↔ Agent 協作紀錄

這個專案由一位工程師（Human）與 Claude Code（Agent）協作完成。這份文件記錄**怎麼分工、Human 在哪裡糾正了 Agent、Agent 在哪裡出錯、以及從中得到的做法**。它與 git history 一起，是「AI / Agent Supervision」的第一手紀錄。

逐步計畫與完成紀錄在 [`MoMO面試/Phase 2 — Implementation.md`](./MoMO面試/Phase%202%20—%20Implementation.md)。

## 1. 工作協議

先設計、後實作；設計文件是 source of truth。

每一個實作步驟固定走這個迴圈，Agent 不跳步、不偷做下一步：

1. **實作** — 有邏輯的部分先寫測試 → 確認紅燈，且是因為**正確的理由**紅 → 最小實作 → 綠燈。
2. **驗證** — 跑該步的驗證指令（驗證關卡不使用快取），貼出實際輸出。失敗照實說。
3. **回報** — 變更清單、驗證結果、有沒有偏離設計以及原因。
4. **Review** — Human 看過才 commit。
5. **Commit** — Conventional Commits，帶 `Co-Authored-By`。每個 commit 後 `main` 都是綠的。

分工原則：**決定權在 Human 的事，Agent 提出建議與理由後停下來等**（架構取捨、要不要公開某個檔案、專案放在哪個磁碟）；**可逆、且在已同意範圍內的事，Agent 直接做並回報**。

## 2. Human 糾正 Agent 的紀錄

Human 共糾正 Agent 十二次。每一次都讓設計變得更好，值得記下來。

| #   | Agent 原本的提案                                                                                                                                                      | Human 的糾正                                                                                                                                                                    | 結果                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| --- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | 詳情頁的三顆按鈕加上互動：放入購物車讓徽章 +1、加入追蹤可切換、直接購買跳提示                                                                                         | 需求筆記明寫詳情頁是展示用、不可擅自往下做。Agent 讀了卻仍然提案超出範圍的東西                                                                                                  | 三顆按鈕只渲染、不綁行為。連帶移除 `cart/data-access` 與 Zustand —— 盤點後全域 client state 為 0（[ADR-0003](./adr/0003-server-state-only-no-global-store.md)）                                                                                                                                                                                                                                                                                                                                                              |
| 2   | 首頁 13 個區塊全部放進同一個 `home/feature` lib                                                                                                                       | 「首頁明明有這麼多區塊，為何不做 features？」                                                                                                                                   | Agent 重新檢視後發現原案確實有缺陷：「你可能會喜歡」在詳情頁也會出現，埋在 `home` 裡 `goods` 就拿不到。改為依「有沒有自己的邏輯 / 資料 / 重用性」切分，並新增 `type:page` 作為唯一能組合 feature 的層                                                                                                                                                                                                                                                                                                                        |
| 3   | 把看起來通用的元件都放進 `shared/ui`（含只有一個使用者的「看更多」按鈕、倒數計時）                                                                                    | 「被兩個以上 feature 共用才該放 shared；只有自己用的應該留在自己的 feature 底下」                                                                                               | 訂出 **Rule of Two** 與 lib 內部的 `ui/`、`model/` 私有資料夾。`shared/ui` 從 10 個元件瘦身到 6 個，同一條規則也套用到 `shared/util`                                                                                                                                                                                                                                                                                                                                                                                         |
| 4   | （實作兩步之後）Agent 認為架構已定案                                                                                                                                  | 「我們大量寫在 lib 上，以後擴充購物車、結帳、品牌頁、刷卡時，libs 不會變得很肥大嗎？」                                                                                          | Agent 承認三個真實弱點與一個事實：以目前規模，這個結構是偏重的。產出 lib 粒度準則、拆分觸發條件與 [ADR-0006](./adr/0006-domain-dependency-map.md)，並把代價照實寫進 README                                                                                                                                                                                                                                                                                                                                                   |
| 5   | 設計 token 的顏色是看截圖估的（品牌色估成 `#e6007f`、文字估成純黑），而且只有一層：語意名稱直接對色碼                                                                 | 「你要到網站去檢查配色、框線、文字顏色與字體大小，不然後續的畫面會極度不像；顏色要用 token design system，並考慮 Primitive / Semantic 兩層」                                    | 以瀏覽器讀取真實網站的計算樣式：品牌色實為 `#d62872`、文字為 `#404040`，價格有三種顏色而不是一種。改為兩層 token，並關閉 Tailwind 預設色盤讓非 token 的顏色用不了。對照表見 [`design-tokens.md`](./design-tokens.md)                                                                                                                                                                                                                                                                                                         |
| 6   | 全站外框叫做 `shell`，架構文件從頭到尾沒有出現「layout」這個詞，也沒有一節說明它的機制；完整的外框實作被排在資料層與共用元件之後                                      | 「會維持的 TopBar 與最下面的 footer 不都屬於 layout 嗎？為何看起來我們沒有設計 layout？到商品詳情頁時這兩個應該還是會被保留才對」                                               | layout 其實已經存在並有測試（layout route + `<Outlet />`），但 Human 看不出來是 Agent 的三個缺口造成的：**命名**（「shell library」在 Nx 社群另有所指）、**文件沒有 Layout 一節**、**順序排錯**（layout 是每一頁的骨架，卻排在很後面）。改名為 `layout`、在 `architecture.md` 補上機制說明、並把 layout 的實作提前到 walking skeleton 之後                                                                                                                                                                                   |
| 7   | 全站外框放在 `libs/layout/feature`，標成 `type:feature`、`scope:layout`                                                                                               | 「layout 裡面卻有 feature，你認為這是好的設計嗎？」                                                                                                                             | 兩個地方都錯。路徑的寫法是 `libs/<誰的>/<哪一種>`，「layout」是種類卻被放在擁有者的位置 —— 這是上一次改名只換了字、沒有重新檢查整條路徑造成的。層級也標錯：外框與 `page` 同層（只有 router 會 import 它、它包住 page），標成 feature 讓它無法組合別的 domain 的 feature。新增 `type:layout` 一層，lib 搬到 `libs/shop/layout`                                                                                                                                                                                                |
| 8   | 引用 Nx 官方範例時寫成「header / layout 放在 lib」，用來支持「layout 該放 lib」                                                                                       | 「應該是 layout 會有 header 跟 footer 吧？不是 header/layout 吧？」，並要求「以 Nx 以往的做法查證」「說老實話」                                                                 | 用詞的糾正同時揭穿了證據的誤讀：`nx-examples` 放進 lib 的只有 header 這個**零件**，layout **外框**在兩個 app 裡各寫一份。Agent 把兩者混為一談，讓證據看起來支持自己。重新查證並比較三種放法後維持原位置，但理由改對、比較與翻盤條件寫成 [ADR-0007](./adr/0007-layout-as-a-lib-and-a-tier.md)                                                                                                                                                                                                                                 |
| 9   | 資料夾叫 `libs/`，而每個專案都有 `package.json`。被問到時 Agent 建議「保留 `libs/`，補一段文件說明就好」                                                              | 「全部都有 package.json 應該是 packages 才對」；決定改名後再追加：「避免只是 libs 改名成 packages 而已，要符合 pnpm 與 Nx 的 package 規範，裡面的 lib 資料夾也要重新規劃」      | Human 的第二個要求才是重點。對照 pnpm、Nx、Turborepo 的文件後，找到三個**改名解決不了**的真問題：package import 了 react 卻沒有宣告（靠根目錄的 `node_modules` 偷渡）、package 內部多一層沒有意義的 `src/lib/`、app 的 CSS 以相對路徑伸進另一個 package。Agent 原本的建議會讓這三個問題繼續存在。寫成 [ADR-0008](./adr/0008-packages-not-libs.md)                                                                                                                                                                            |
| 10  | Step 6 回報時說「moPro 不是商品列」，並把暢銷榜、區塊標題等設計當成已確定的事實                                                                                       | 「需要你對一下頁面實際內容，再重複看一下是否符合你剛說的設計方式」                                                                                                              | 以桌機尺寸載入真站、逐區塊讀 DOM 對照。**核對無誤的**：分類面板 40 個、每列 9 個、一列一種色調；區塊順序；猜你想搜是「圖 + 關鍵字」；限時搶購卡片的欄位；三種價格顏色。**錯的有三項**：(1) moPro 在真站上每張圖都連到商品頁 —— 「用圖磚呈現」是對的，「不是商品」是錯的；(2) 今日暢銷榜**沒有名次徽章**，那是規劃時自己加的，還被寫成一條行為規格；(3) 區塊標題量不到不是因為「在跨來源 iframe 裡」，而是它根本是一張背景圖。另外得知商品卡有直式與橫式兩種版型。規格、設計文件與步驟都已修正，差異列進 README 的 Known Gaps |
| 11  | 程式碼註解寫成了設計說明文：英文、動輒五到十行，內容是設計沿革、取捨的論述、「誰在用它」、「怎麼驗證的」。全專案 118 個檔案共 868 行註解                              | 「這完全不是讓人懂的註解。需要的是：對照資訊（例如 SectionRegistry 每一項對應哪一塊）、太難的流程、重要性高或太難的函式才寫；要簡單扼要，而且寫中文。對開發者無用的資訊不要放」 | 用腳本把全專案的註解區塊連同下一行程式碼抽出來逐塊處理：刪除，或改寫成一行中文。868 行 → 418 行，全部中文；並替 5 個原本沒有註解的難懂片段補上一行（雜湊產生價格、分類底色的計算、mock 的分頁、假的 embla、輪播的狀態同步）。規則寫進 `CLAUDE.md`。Agent 把「為什麼這樣設計」寫進程式碼，是把 commit message、ADR 與註解三種東西混為一談：**註解回答「這段程式碼在做什麼、有什麼陷阱」，設計理由屬於 docs**                                                                                                                  |
| 12  | Step 12 照方案 A 把今日暢銷榜改成 `product-rail` 的一筆設定時，切法的依據只有目標截圖，以及「自己的 mock 裡 `getRanking()` 剛好就是 `getCollection('best-sellers')`」 | 「我認為你需要到 momo 首頁抓取裡面內容，再回來思考這段切法是否符合你現在設計的想法」                                                                                            | 到真站讀 DOM：今日暢銷榜與 momo 店取是**同一種 CMS 區塊**（`bt_7_777_01` / `_02`、商品卡 class 完全相同），方案 A 成立；而且得到兩個只看截圖看不到的修正 —— (1) 粉色底是每個區塊容器都有的 inline 背景色，所以它該是版位資料的欄位，不是元件的變體，也不是 token；(2) 橫式商品卡的框線是 `#d9d9d9`、沒有陰影，Step 8 起 momo 店取一直沿用直式卡量到的樣式，是錯的。**切法的依據要是「對方的系統怎麼切」，不是「我的 mock 剛好接得上」**                                                                                      |

共同的模式：**Agent 擅長把一個方向做完整，但不擅長質疑方向本身。** 這些糾正多半是 Human 從「這樣以後會怎樣」的角度提問，而不是指出某一行寫錯。

## 3. Agent 的自我審查（設計 v1 → v2）

Human 要求 Agent 在定案前重新審查自己的設計。Agent 找到 8 個問題，其中影響最大的三個：

1. `ProductCard` 沒講清楚吃什麼型別 → 直接吃 `Product` 就會變成 `ui → data-access`，違反自己訂的分層。改為 ui 宣告最小形狀、靠 structural typing 傳入。
2. hooks 直接 import repository singleton → 「只換一個實作檔」其實做不到，測試也只能吃 55 筆真 fixture。改為 Context 注入。
3. 交付順序是水平分層（util → ui → data → …）→ 時間用完時可能連詳情頁都還沒出現。改為 walking skeleton 先行，不可砍的先做。

完整 8 點見[設計筆記](./MoMO面試/Phase%201%20—%20%20Design%20and%20Planning.md)開頭。

## 4. Agent 出錯與事故紀錄

| 事件                                  | 發生了什麼                                                                                                                                                                                                                                                                                                                                                             | 怎麼發現                                                                                                                                                     | 之後的做法                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| ------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 並行編輯                              | Agent 寫入一份筆記時，Human 同時在 Obsidian 編輯同一個資料夾；另一份筆記的內容被覆蓋。Agent 發現異常後只口頭提了一句，就繼續往該檔案追加內容                                                                                                                                                                                                                           | Human 發現筆記內容不對                                                                                                                                       | 用 vault 的 git 備份逐位元組還原並以 hash 驗證。此後寫入共用檔案前先確認檔案仍是預期狀態（hash / 關鍵字），不符就中止；**發現異常要先停下來確認，而不是繼續做**                                                                                                                                                                                                                                                                                    |
| tags 靜默失效                         | PowerShell 把 `--tags=type:util,scope:shared` 的逗號當成陣列分隔，tags 變成單一字串 `"type:util scope:shared"`。boundary 規則對這個 lib 完全失效，而且不會報錯                                                                                                                                                                                                         | Agent 產生第一個 lib 後檢視產出才發現                                                                                                                        | 參數加引號；**不信任 generator 的輸出，逐一核對全部 10 個 lib 的 tags**；再用負向驗證確認規則真的會擋                                                                                                                                                                                                                                                                                                                                              |
| 誤判為通過的風險                      | 全量驗證時 `shop:test` 失敗。原因是機器忙碌時 Vitest worker 啟動逾時，不是測試沒過                                                                                                                                                                                                                                                                                     | 讀錯誤訊息而不是只看 exit code                                                                                                                               | 單獨重跑確認為綠才算通過，並在回報中說明；之後驗證一律 `--parallel=1`                                                                                                                                                                                                                                                                                                                                                                              |
| 即將公開個資                          | 17 張截圖中有 2 張是登入狀態，頂部列有遮罩後的帳號姓名                                                                                                                                                                                                                                                                                                                 | Agent 在推上 public repo 前把每張圖都看過                                                                                                                    | 只在 repo 的複本上遮蓋，原圖不動；由 Human 決定是否保留遮蓋。原則：**不可逆的動作（公開）選保守的一邊，可逆的那一邊留給 Human 決定**                                                                                                                                                                                                                                                                                                               |
| 假綠燈：lockfile 過期                 | commit 進去的 `pnpm-lock.yaml` 缺了 8 個 lib 的 importer 條目。本機因為 `node_modules` 已存在而一切正常，但全新 clone 後 `pnpm install --frozen-lockfile`（CI 的預設）會直接失敗。成因：pnpm 12.4.2 寫 lockfile 時不會為「沒有宣告依賴的新 workspace 專案」補條目，檢查時卻要求每個專案都有 —— 它自己產生的 lockfile 通不過自己的檢查                                  | 在另一顆磁碟建立沒有 `node_modules` 的測試複本時，第一次安裝就失敗                                                                                           | 補齊條目並在全新環境以 `--frozen-lockfile` 驗證通過。**「在我的機器上是綠的」不等於「可重現」**：驗證要在乾淨環境跑一次。新增 lib 後要確認 lockfile 有它的條目                                                                                                                                                                                                                                                                                     |
| 診斷時的錯誤                          | 追查上一項時，Agent 用來列出 lockfile 條目的 regex 寫錯（漏掉 `path: {}` 這種格式），因而誤判「只有 1 個條目」，並據此在測試複本上插入了重複的 key                                                                                                                                                                                                                     | pnpm 回報 duplicate key                                                                                                                                      | 直接讀原文確認，而不是依賴自己寫的 regex 推測。錯誤的修改只發生在測試複本上，沒有碰到基準 —— 這正是複本存在的理由                                                                                                                                                                                                                                                                                                                                  |
| 規格寫了沒驗證過的行為                | 第一版 OpenSpec 聲稱「以深層路徑引用另一個 lib 的內部檔案時 lint 會失敗」，但先前的負向驗證只測了 5 種違規，不包含這一種。Human 要求重新檢查後補測：相對路徑確實被 lint 擋下，但「套件名稱 + 內部路徑」**不是 lint 擋的**，而是型別檢查（TS2307，因為 `package.json` 的 `exports` 只公開入口）                                                                         | Human 要求以另一個模型重新審查規格                                                                                                                           | 規格依實際結果改寫；`openspec/config.yaml` 加入規則：**聲稱「會被工具擋下」的行為必須實際放入違規樣本驗證過才能寫進規格**。驗證關卡改為 `lint` 與 `typecheck` 都要跑                                                                                                                                                                                                                                                                               |
| 說了要做卻沒做                        | Agent 向 Human 說明 OpenSpec 的規劃時，表示會在 `config.yaml` 寫入專案脈絡、並把已實作的約束寫成主規格；實際交付時 `config.yaml` 只有語言設定，已實作的約束也被放進 change 當成「待新增」。交付報告沒有提到這個落差                                                                                                                                                    | 同上，重新審查時對照自己先前的說明                                                                                                                           | 補上 `config.yaml` 的脈絡與撰寫規則；`module-boundaries` 移到主規格。**交付前對照自己承諾過的清單，而不是只對照驗證工具的結果** —— 格式驗證通過不代表內容符合當初說好的範圍                                                                                                                                                                                                                                                                        |
| 計畫中的項目漏做                      | Step 1 列了「`package.json` 加 `engines.node`」，實際沒做，完成紀錄也沒提                                                                                                                                                                                                                                                                                              | Human 要求把已完成的步驟打勾時，逐項核對才發現                                                                                                               | 已補上（依 Vite 8 的實際要求）。**打勾要逐項核對，不是整步一起勾**                                                                                                                                                                                                                                                                                                                                                                                 |
| 量測工具丟掉透明度                    | 從真實網站讀顏色時，Agent 自己寫的「rgb → hex」轉換函式有好幾個版本，其中一個會把 alpha 丟掉。量分類膠囊時讀到 `#e1eff6`，後來改讀原始值才發現是 `rgba(225, 239, 246, 0.5)` —— 差一半的透明度                                                                                                                                                                          | 換一種讀法（直接取 `getComputedStyle` 的原始字串）時數值對不上                                                                                               | 膠囊的 token 改用 `color-mix` 保留透明度。回頭檢查哪些數值是用有問題的版本量的（footer 與倒數數字共四個），**複核不了的就照實標成「未複核」**，而不是假設它們沒問題                                                                                                                                                                                                                                                                                |
| 同一個錯誤犯第二次                    | PowerShell 對單一字串取 `[0]` 會得到第一個字元而不是整行。Agent 在 Step 4 犯過、也寫進了這份紀錄，到 Step 5 更新文件時又犯了一次，四項文件修改因此沒有寫入                                                                                                                                                                                                             | 指令回報型別轉換錯誤                                                                                                                                         | 改用 `@(...)[0]` 強制成陣列，並加上「已套用過就中止」的防重複檢查後重跑。**把錯誤寫進紀錄不等於不會再犯** —— 真正有效的是把正確寫法變成習慣，或是改用不會有這個陷阱的工具                                                                                                                                                                                                                                                                          |
| 提案理由與自己的決策紀錄矛盾          | 向 Human 提議把 layout 升為一層時，理由之一是「header 的購物車數量會是一個 feature，現在放不進來」。ADR-0006 早就寫明外框只透過 `data-access` 讀摘要資料 —— 數量徽章不需要 feature。Agent 提案時沒有回頭讀自己寫的 ADR                                                                                                                                                 | 動手改 ADR 時讀到那一句                                                                                                                                      | 沒有悄悄換字：向 Human 說明說得太滿，重新釐清分界（**摘要資料走 `data-access`；別的 domain 擁有的互動元件才是 feature**），把「為什麼推翻原本那句話」寫進 ADR。提案前先讀相關的決策紀錄                                                                                                                                                                                                                                                            |
| 探針假通過                            | 驗證新規則的 5 個探針中，「feature → layout」回報通過。原因是探針檔沒有寫進去：那個 lib 還是空殼，沒有 `src/lib/` 資料夾，寫檔失敗後 lint 檢查的是乾淨的 lib                                                                                                                                                                                                           | 結果與預期不符，回頭看到寫檔的錯誤訊息                                                                                                                       | 沒有把它當成通過；確認探針檔存在後重跑，確認被擋。**驗證工具回報「沒問題」時，先確認它真的檢查了東西** —— 同一天稍早檢查 tsconfig 時也先用故意寫壞的樣本確認過檢查本身有效                                                                                                                                                                                                                                                                         |
| 同一個決定換了三次建議                | layout 該放哪，Agent 依序建議：維持 lib → 外框搬進 app → 維持 lib。過程中說錯三句話：(1)「shell library 在 Nx 另有所指」先說得太滿、又更正過頭成「我們是不含路由的 feature-shell」—— feature-shell 的定義就是擁有路由，拿掉就不是了；(2)「拆掉 `type:layout` 之後全部是 Nx 標準 type」—— `type:page` 本來就是自訂的；(3) 把約 12 行排版 JSX 的去留講得像重大的架構差異 | Human 連續追問，最後要求「說老實話」                                                                                                                         | 每一輪都是先有結論再找理由，直到被要求查證才去讀 Nx 的文件與範例原始碼。**之後遇到「放哪裡」這類有業界慣例的問題，先查證再提案**；差距很小時要直接說「差距很小」，並把改動成本與還沒做的工作算進去 —— 首頁與商品詳情頁當時一步都還沒開始                                                                                                                                                                                                           |
| 探針連續兩輪給出假的「被擋下」        | 驗證 `@nx/dependency-checks` 時，修改 `package.json` 的四個探針都回報被擋。實際上第一輪是 **pnpm** 發現 `package.json` 與 lockfile 不一致、在 CI 模式下拒絕執行，ESLint 根本沒跑；改成直接呼叫 nx 之後，第二輪是**執行檔路徑寫錯**，每個探針都因為找不到模組而失敗。探針腳本只看 exit code，兩輪都印出整排 OK                                                          | 第一輪：印出來的錯誤訊息是 `tslib` 而不是預期的 `react`，而且有一個探針與預期不符，Agent 才去看原始輸出。第二輪：同樣去看原始輸出                            | 探針改為「輸出裡必須出現那條規則的名稱才算被擋，否則判為 BROKEN」。第三輪才是真的。**負向驗證的失敗必須是因為正確的理由** —— 這和 TDD 的「紅燈要因為正確的理由而紅」是同一件事，Agent 在 TDD 上有做到，在探針上沒有。已寫進 `CLAUDE.md`                                                                                                                                                                                                            |
| 規則開著卻什麼都不檢查                | `@nx/dependency-checks` 照預設值啟用後 lint 全綠。讀了規則的原始碼才知道：專案若沒有 `buildTargets` 所列的 target 它就直接 return，而預設值 `['build']` 只有 app 有 —— 10 個 package 完全沒有被檢查                                                                                                                                                                    | 啟用前先讀了規則原始碼（因為上一則向 Human 承諾過「還沒驗證它對不 build 的 package 有沒有效」）                                                              | 改用每個專案都有的 `typecheck`；`verify-boundaries` 新增檢查「規則是 error，而且專案真的有那個 target」，並做了自我測試。和 Step 2 的「tags 寫壞 → 規則靜默失效」是同一類問題：**會靜默失效的規則，要有另一個工具確認它真的在運作**                                                                                                                                                                                                                |
| 素材盤點漏掉巢狀資料夾                | Step 6 一開始盤點素材時，每個資料夾只列了第一層的檔案，沒看到 `主要活動/今日大牌`。Phase 1 §7 其實早就規劃了它的 slug                                                                                                                                                                                                                                                  | 匯入工具被設計成「每個來源檔案都要有交代，否則 exit 1」，第一次執行就報出這個沒有 slug 的資料夾；幾乎同時 Human 也提醒「每個區塊要放的圖都給你了，要確認好」 | 改為從素材根目錄遞迴處理，並以內容雜湊對帳：203 個來源檔案，202 個複製、1 個因為與同資料夾的檔案逐位元組相同而略過。**盤點要用工具做、而且要能失敗**；用眼睛看資料夾清單會漏                                                                                                                                                                                                                                                                       |
| 測試自己的 bug：`beforeEach` 的回傳值 | `beforeEach(() => mock.mockClear())` 讓三個測試失敗，錯誤是 mock 被以 `undefined` 呼叫。實作是對的：Vitest 把 `beforeEach` 回傳的東西當成清理函式執行，而 `mockClear()` 回傳 mock 本身                                                                                                                                                                                 | 實作完成後測試仍然是紅的，讀錯誤訊息的堆疊指向 spec 檔而不是實作                                                                                             | 加上大括號並留下註解。紅燈不一定是實作的錯，先看堆疊指向哪裡                                                                                                                                                                                                                                                                                                                                                                                       |
| 規格裡有一條自己編的 requirement      | `home-page` 規格的「今日暢銷榜顯示名次」、`design.md` 的「名次徽章」、`feature-ranking` 的 README 與 Step 12 的 `rank-badge`，全部來自 Agent 對「暢銷榜」這個詞的聯想。目標截圖上沒有名次，真站也沒有                                                                                                                                                                  | Human 要求對照真站後，Agent 重看截圖才發現                                                                                                                   | 改寫 requirement 為實際的樣子（橫式商品卡、沒有名次）。**寫規格時，每一條 requirement 都要能指出它在截圖或真站上的依據**；指不出來的就是編的                                                                                                                                                                                                                                                                                                       |
| 把猜測寫成原因                        | 區塊標題的字級量不到時，Agent 在 `design-tokens.md` 與 token 的註解寫「推測位於跨來源的 iframe 內」。實際原因是標題是一張 1220×70 的背景圖。這個猜測從 Step 4 一路留到 Step 6，期間被複述了好幾次                                                                                                                                                                      | 這次對照真站時，發現每個區塊的第一條連結都是 1220×70、沒有文字也沒有 `<img>`，才去讀它的 `background-image`                                                  | 文件與註解已改成查證後的事實。「推測」兩個字不能當成免責：**沒驗證的原因不要寫進文件**，寫「原因不明」就好                                                                                                                                                                                                                                                                                                                                         |
| 用推理代替驗證第三方套件的行為        | 為 `Carousel` 寫 jsdom 的測試時，Agent 讀了 embla 的原始碼，判斷「沒設 breakpoints 就不會呼叫 `matchMedia`」，只補了 `ResizeObserver` 與 `IntersectionObserver` 的 stub。實際上 embla 無條件執行 `[].map(ownerWindow.matchMedia)`：陣列是空的，但 callback 是 `undefined` 時 `map` 照樣 throw。而且它是從元素自己的 window 取 `matchMedia`，`vi.stubGlobal` 寫不到那裡 | 「用真的 embla 掛載一次」的 spec 第一次執行就失敗，堆疊指向 embla 的 `OptionsHandler`                                                                        | 補上 stub 並把原因寫在 spec 的註解裡。這個 spec 本來是為了「升級套件時 API 改名會被發現」而留的，結果先抓到的是 Agent 自己的推論錯誤。**讀原始碼得到的結論仍然是假設，跑過才算**                                                                                                                                                                                                                                                                   |
| 「不顯示」類的測試在紅燈階段空洞通過  | `PriceTag` 的 2 個 spec（原價不高於售價時不顯示劃線價）與 `Carousel` 的 3 個 spec（不顯示箭頭、不顯示圓點 ×2）只斷言「某個東西不存在」。對「什麼都不渲染」的空實作，它們全部通過 —— 也就是說，元件整個壞掉時它們也會通過                                                                                                                                               | 紅燈階段逐一看哪些 spec 沒有紅                                                                                                                               | 每一個都補上「該有的東西存在」的斷言（售價有顯示、slides 有渲染）。補完當下 Agent 直接去寫實作，沒有回頭確認它們真的會紅；寫紀錄時寫到「重新確認紅燈」才發現這件事沒做，於是補做：把兩個元件暫時換回空實作重跑，`PriceTag` 5 / 5、`Carousel` 11 / 11 全紅，再用 git 還原。**只斷言「不存在」的測試，要同時斷言一個「存在」，否則元件消失也會通過**                                                                                                 |
| 同名的巢狀 landmark                   | `Carousel` 的根元素是 `<section aria-label>`（landmark region）。首頁的每個輪播都放在一個同名的區塊裡，螢幕閱讀器使用者會連續聽到兩次「降價好貨，區域」。Step 7 單獨測 `Carousel` 時看不出來，因為那時它外面沒有東西                                                                                                                                                   | app 的整合測試要找名為「主要活動」的 region，Testing Library 回報「找到多個」                                                                                | 依 WAI-ARIA 的輪播模式改為 `role="group"`。**這是測試抓到的，不是 Agent 讀程式碼看出來的** —— 用角色與名稱查詢的測試，順帶檢查了無障礙結構；用 `data-testid` 查就不會發現                                                                                                                                                                                                                                                                          |
| `'matchMedia' in window` 是 true      | 把輪播的測試環境補丁搬到共用入口時，Agent 加了「已經有就不覆蓋」的判斷，寫成 `name in target`。jsdom 的 window 上有 `matchMedia` 這個屬性，但值是 `undefined`，補丁因此被跳過                                                                                                                                                                                          | 搬完之後原本通過的 embla spec 立刻失敗，錯誤訊息和 Step 7 那次一模一樣                                                                                       | 改為 `typeof target[name] === 'function'`。重構時「順手加的防護」也是新程式碼，一樣要跑過才算                                                                                                                                                                                                                                                                                                                                                      |
| 瀏覽器面板在背景時的量測與截圖        | Step 8 到真站量尺寸時，圖片高度全部是 0、好幾個區塊找不到；回到自己的 dev server 截圖時畫面錯位或逾時。原因都是預覽面板在背景（`document.hidden`）：真站的圖不載入、`loading="lazy"` 的圖不載入、畫面不重繪                                                                                                                                                            | 量到的數字不合理（高度 0），於是先查 `document.hidden`                                                                                                       | 改量不依賴圖片載入的東西（容器寬度與間距；圖的比例從素材檔案讀）；自己的頁面改用 DOM 讀幾何與行為。**沒有完成的是「用眼睛對照截圖」**，已照實寫進 `tasks.md` 與回報，請 Human 在瀏覽器看一次                                                                                                                                                                                                                                                       |
| 過期的 closure 當成防重複的依據       | Step 6 寫的 `useRecommendations` 用 `isFetchingNextPage` 擋重複的 `loadMore`，當時的 hook 測試也通過。Step 9 的元件測試連點三下，repository 被呼叫 4 次而不是 2 次：連點發生在 React 重新 render 之前，closure 裡的值還是 `false`；而 `fetchNextPage` 預設會取消進行中的請求重來                                                                                       | Step 9 照規格的 scenario「載入中重複點擊」寫的 spec 失敗（預期 2 次、實際 4 次）                                                                             | 改用 `fetchNextPage({ cancelRefetch: false })`，並在 hook 層級補一個「同一個 `loadMore` 連續呼叫三次」的 spec（修正前同樣紅燈）。Step 6 的測試每次呼叫之間都等了 render，所以測不到。**防重複不能靠 render 出來的狀態**，要靠呼叫當下就成立的機制                                                                                                                                                                                                  |
| 整合測試裡寫死的商品 id 其實不存在    | `router.spec.tsx` 從 walking skeleton 起就用 `/goods/15687497` 測詳情頁。placeholder 只是把 id 印出來，所以一直是綠的。Step 10 換成真的頁面後，這個 id 在 fixture 裡查不到（它是規格範例裡的數字，不是素材裡的商品）                                                                                                                                                   | 動手改 spec 前先 grep 了 fixture，結果是 0 筆                                                                                                                | 改為向 app 注入的同一個 mock repository 要一件真的商品。**測試資料要嘛自己注入、要嘛從來源取得**，不要手抄一個「看起來像」的值                                                                                                                                                                                                                                                                                                                     |
| 註解裡的字被 Tailwind 當成 class      | 為了確認「只動了註解」，Agent 比對改動前後的 build 產物，預期雜湊完全相同，結果不同。追查後：JS 在正規化檔名雜湊後逐位元組相同；CSS 少了 `.static` 與 `.visible` 兩個 utility —— **Tailwind 連註解也會掃**，原本的英文註解裡剛好出現這兩個字，於是產生了沒有人用的 class                                                                                               | 產物雜湊和預期不符，沒有直接當成「應該沒事」，而是把兩份產物留下來比內容                                                                                     | 確認沒有元件使用那兩個 class（只有 `focus-visible:` 前綴）。中文註解不會有這個問題。**「預期相同卻不同」要查到原因為止**，這次的原因剛好無害，下次不一定                                                                                                                                                                                                                                                                                           |
| 背景分頁裡的動畫不會前進              | Step 11 在瀏覽器驗證限時搶購換頁時，量測腳本回報「按了下一頁，slide 沒有移動」。有兩個原因疊在一起：(1) Agent 的腳本用 slide 的左緣判斷目前頁，沒有把 slide 的 `padding-left`（輪播的間距就放在那裡）算進去，判斷本身是錯的；(2) 預覽面板在背景，瀏覽器暫停 `requestAnimationFrame`，embla 的捲動動畫真的沒有前進                                                      | 量到的結果互相矛盾（圓點與按鈕的狀態變了，位移卻是 0），回頭檢查腳本與 `document.hidden`（為 `true`）                                                        | 改驗不依賴動畫的狀態：目前頁的圓點 2 → 3 → 2、最後一頁「下一頁」停用。**「slide 實際位移」這次沒有驗到，照實寫進 `tasks.md` 與回報**，沒有因為狀態正確就寫成「換頁正常」。同一個 `Carousel` 的位移在 Step 7 面板在前景時驗過。倒數不受影響：它每次 tick 重新讀時鐘，被延後的計時器只會少跳幾格、不會算錯 —— 這正是 `use-countdown` 不用「減一」的理由                                                                                              |
| 第一次 CI 是空的綠燈                  | Step 13 加上 CI 後第一次執行 29 秒就綠了。`nx affected` 要和「上一次成功的 CI」比，第一次沒有得比，退回 `HEAD~1`；而那個 commit 只新增了 workflow 檔，所以沒有任何專案受影響 —— lint、test、typecheck、build **一個 task 都沒跑**。更糟的是這個空的綠燈會成為之後每次比較的基準：在它之前寫的程式碼永遠不會在 CI 上跑到                                                | 29 秒太快，不合理，去讀 log 看到 `No tasks were run`                                                                                                         | push 到 main 改為跑全部專案（main 的綠燈要代表整個 repo 是好的），PR 才跑 affected。**這是同一類問題第四次出現**（tags 靜默失效、`dependency-checks` 什麼都不檢查、探針假通過）：回報成功的檢查，要先確認它真的檢查了東西。這次是靠「數字不合理就去看原始輸出」抓到的                                                                                                                                                                              |
| 驗證指令掛了 20 分鐘，Agent 沒發現    | Step 13 在另一顆磁碟的全新 clone 上照 README 跑 `pnpm nx run-many …`，放在背景並把輸出接到過濾用的管線。指令本身約 2 分鐘就成功了，但 Agent 這次照 README 原樣執行、沒有加平常在這台機器上用的 `NX_DAEMON=false`：Nx 啟動的常駐 daemon 繼承了那條管線，管線等不到結束，整個背景工作就一直掛著。Agent 只是在等「完成通知」，沒有設任何逾時或進度檢查                    | **Human 問：「為什麼跑這麼久都還沒好？超過 20 分鐘了」**                                                                                                     | 查行程：只剩 daemon 活著、Nx 的工作紀錄 18 分鐘前就停止寫入 → 停掉 daemon，被卡住的結果立刻吐出來（10 個專案全過），其餘步驟改用 `NX_DAEMON=false` 在前景跑完。README 的指令沒有問題：在一般終端機裡指令會正常返回，daemon 留在背景是 Nx 的設計；有問題的是 Agent 擷取輸出的方式。**放到背景的工作要有預期的完成時間，超過就去查，而不是繼續等通知** —— §6 早就記了這台機器上 Nx 要關 daemon，Agent 在「照 README 原樣跑」的時候忘了自己的環境備註 |

## 5. 與原計畫的偏離

| 步驟       | 偏離                                                                                                                               | 原因                                                                                                                                                                                                                                                                           |
| ---------- | ---------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Scaffold   | 沒有使用 Nx 的 `react-monorepo` preset                                                                                             | Nx 23 的 preset 改為下載官方範本並忽略 flags；下載到的是一個完整的示範電商專案（含 `feature-products`、`feature-product-detail`）。領域與本題相同，直接使用會讓審閱者看到的是 Nx 的架構而不是我們的設計。改為「空 workspace + generator」                                      |
| Scaffold   | 移除範本附帶的約 70 個檔案                                                                                                         | 多種 AI 工具的設定、一份會自動啟用第三方 plugin 市集的 `.claude/settings.json`、一份使用 npm + Nx Cloud 而必然失敗的 CI。原則：repo 裡的每個檔案都要是我們說得清楚的                                                                                                           |
| Scaffold   | 採用 pnpm workspaces + TS project references，而非 tsconfig paths                                                                  | 範本模式下 `--workspaces=false` 無效。這是 Nx 目前的標準做法，`@momo/*` 由各 lib 的 package name 提供，設計不受影響                                                                                                                                                            |
| Scaffold   | pnpm `allowBuilds` 只核准 `nx` 與 `@swc/core`                                                                                      | pnpm 12 預設不執行依賴的安裝腳本。用明確的 allow-list 而不是 `--all`，之後有新套件想執行腳本時仍會被擋下來判斷                                                                                                                                                                 |
| Libs       | 3 個 feature lib 的 placeholder 延到真正需要時才加                                                                                 | 目前用不到                                                                                                                                                                                                                                                                     |
| Libs       | 空的 lib 設了 `passWithNoTests`                                                                                                    | Vitest 找不到測試檔會判定失敗。該 lib 出現第一個 spec 時要移除                                                                                                                                                                                                                 |
| Skeleton   | 裝到的是 React Router **8**，不是設計文件寫的 v7                                                                                   | `pnpm add react-router` 取得當時的最新主版號。沒有憑 v7 的印象寫：先讀實際安裝版本的型別定義，確認 `createBrowserRouter`、`RouterProvider`（自 `react-router/dom`）、route 的 `lazy` 都還在才動手。v8 要求 Node `>=22.22.0`，`engines` 隨之收緊；所有文件的版本號已更正        |
| Skeleton   | app 與 9 個 React lib 的 tsconfig 補上 DOM 型別庫                                                                                  | generator 產生的設定沒有 `lib: dom`，元件拿到的是 React 型別附的空殼 `HTMLElement`。lint 與測試都是綠的，只有 `typecheck` 抓到（TS2812）—— 三種檢查缺一不可的實例                                                                                                              |
| Skeleton   | 字體也成為 token（`--font-sans`）                                                                                                  | 真實網站用的是舊版 Tailwind 的 sans 堆疊；Tailwind 4.3 改了自己的預設值。量到的數值就該釘住，而不是繼承工具的預設                                                                                                                                                              |
| 資料層     | moPro 區塊不產生商品資料（Phase 1 規劃它是商品列）                                                                                 | 打開素材才知道：那個資料夾的 10 張圖是**整張做好的促銷磚**，品牌、品名、價格都印在圖上。用商品卡顯示的話，圖下方會再重複一次名稱與價格。它應該是 banner 輪播，Step 8 的版位設定照這個做；行為規格沒有把它定義成商品列，所以規格不用改                                          |
| 資料層     | 分類清單手寫，不由產生器產生                                                                                                       | 計畫寫的是產生器一併產生 `categories.ts`。但分類是從真站讀來的，沒有任何素材可以推導出它                                                                                                                                                                                       |
| 資料層     | `Category` 不帶色調，色調由 layout 依位置決定                                                                                      | layout 私有的清單原本每筆都帶 `tone`。搬進 catalog 時才看出五種底色純粹是「第幾列」（每列 9 個）—— 那是版面的事，不該進 domain model                                                                                                                                           |
| 資料層     | 一份商品表 + 集合只存 id，而不是每個區塊各一份商品清單                                                                             | 同一個商品 id 出現在多個區塊的素材裡。各存一份的話，同一件商品在首頁兩個區塊、或首頁與詳情頁之間的名稱與價格會對不起來                                                                                                                                                         |
| 資料層     | 商品的品牌用虛構的                                                                                                                 | 產生出來的名稱旁邊那張照片並不是那個商品；用真實品牌會像是一頁錯誤的真商品                                                                                                                                                                                                     |
| 共用元件   | `ProductCard` 沒有做計畫裡的 `topBadge` 與 `priceLabel` 兩個 slot                                                                  | `topBadge` 原本唯一的使用者是名次徽章，而名次徽章並不存在；動工前重看截圖確認限時搶購的「搶」在卡片底部那一列、不在圖上。`priceLabel`（「限搶價」）、浮起的外框、紅色 23px 的價格只屬於限時搶購（Step 11，可砍），跟著它一起加。原則：**選項跟著第一個使用者出現**，不預留位置 |
| 共用元件   | 外觀用 `layout` / `frame` / `priceTag` 三個視覺選項表達，計畫裡只有 slots                                                          | 對照真站後知道卡片有直式、橫式兩種版型，外框與價格樣式也各不相同，這些不是 slot 塞得進去的。它們是視覺性質，不是業務概念（沒有 `variant="flash-sale"`），所以不違反「`shared/ui` 不認識業務」                                                                                  |
| 共用元件   | `ProductCard` 收 `href`，而不是自己呼叫 `paths.goods(id)`（`tasks.md` 原本的寫法有歧義）                                           | 由呼叫端組網址，`shared/ui` 就不需要認識路由，`ProductCardItem` 也不需要 `id` 欄位                                                                                                                                                                                             |
| 共用元件   | `Carousel` 的測試分兩份：對假的 embla API 測包裝邏輯、用真的 embla 只測「掛載得起來」                                              | jsdom 不做排版，真的 embla 永遠只回報一頁、無處可捲，換頁邏輯在 jsdom 裡測不到。真正的換頁用一個暫時的探針頁在瀏覽器實測（沒有 commit）                                                                                                                                        |
| 共用元件   | 劃線原價只用一個顏色 token                                                                                                         | 真站依區塊用了三種幾乎一樣的灰（`#999` / `#9ca3af` / `#b3b3b3`）。同一個角色一個值；`ink-subtle` 因此暫時沒有使用者，Step 9 再決定去留                                                                                                                                         |
| 首頁       | 品牌折扣與猜你想搜是 `banner-carousel`，計畫寫的是 `banner-grid`                                                                   | 目標截圖上兩者都有左右箭頭與圓點。計畫是憑印象寫的，動工前重看截圖才發現                                                                                                                                                                                                       |
| 首頁       | 版位資料多了版面數值：`perView`、`gap`、`columns`、`card`、`Banner` 的 `width` / `height`、carousel 與 grid 的 `label`             | 要讓一個 `banner-carousel` 撐起 5 個區塊，差異就得放在資料裡。圖的原始尺寸讓瀏覽器先留位置，輪播載入時不會跳動。`label` 是因為沒有標題的區塊也需要無障礙名稱（grid 原本用第一張圖的 alt 當後備，得到「超大牌 1」）                                                             |
| 首頁       | 錯誤回報由 app 的 `QueryCache` 負責，不在 `HomePage` 裡                                                                            | 計畫只寫「接到 QueryClient 的 onError」，但規格又寫「首頁載入失敗要回報」。兩邊都報會重複；集中在 QueryCache 的話，每個失敗的查詢一次、帶 key，頁面不用記得報                                                                                                                  |
| 首頁       | 沒做 `track()`                                                                                                                     | 沒有呼叫者（同 Step 7 的原則）                                                                                                                                                                                                                                                 |
| 首頁       | layout 的 `<main>` 不再替頁面決定寬度與留白                                                                                        | 首頁是滿版灰底加 1220px 的白色區帶，放不進原本「1220px + padding」的 `<main>`。寬度與底色是頁面自己的事                                                                                                                                                                        |
| 首頁       | `Product` 加了選填的 `promoText`                                                                                                   | 規格寫橫式商品卡帶一行促銷文字，但只有限時搶購的商品有。Step 7 回報時提出，Human 確認後照建議做                                                                                                                                                                                |
| 首頁       | `SectionRenderer` 的 registry 由 props 傳入                                                                                        | 計畫寫「純元件（吃 sections props）」，但若 registry 是 import 進來的，測它就得把 6 個 block、3 個 feature 與它們的 provider 全部帶進來                                                                                                                                        |
| 首頁       | 官方優惠沒有黃色底                                                                                                                 | 截圖當天是活動檔期的樣式；Step 8 實測當天真站是白底。不是版位資料該表達的東西                                                                                                                                                                                                  |
| 推薦區     | 測試用規格自己的數字（55 / 10 / 15 件、每批 15），不是計畫寫的「7 筆、每頁 3 筆」                                                  | 頁面大小是 feature 內部的常數，不值得為了測試開一個 prop。fake repository 照 `offset` / `limit` 切資料，規格的 scenario 就能一條對一條地寫成測試                                                                                                                               |
| 推薦區     | 多了兩個規格以外的失敗情境                                                                                                         | 規格沒寫載入失敗怎麼辦。最小而合理的行為：完全載不到 → 區塊不顯示；後面的批次失敗 → 已顯示的保留、按鈕保留（再按一次就是重試）。各有一個 spec                                                                                                                                  |
| 推薦區     | 「看更多」載入中用 `aria-disabled`，不用 `disabled`                                                                                | `disabled` 的按鈕會失去鍵盤焦點，每載入一批使用者都要重新找位置。重複點擊由資料層保證無害                                                                                                                                                                                      |
| 推薦區     | 商品卡沒有星等、標籤、總銷量                                                                                                       | 資料模型沒有這些欄位；不為了畫面去編更多假資料。列入 Known Gaps                                                                                                                                                                                                                |
| 詳情頁     | 多了載入中、載入失敗、跟著 `goodsId` 變三個情境                                                                                    | 規格只寫存在 / 不存在。前兩個是任何用 query 的頁面都有的狀態；第三個是因為 id 由 props 傳入，從一件商品換到另一件時頁面不會重新掛載                                                                                                                                            |
| 詳情頁     | 價格沒有重用 `PriceTag`                                                                                                            | 真站詳情頁的寫法是「促銷價 50,200 元」、沒有 `$`，和商品卡是兩種格式。硬塞進 `PriceTag` 會讓共用元件多出只有一個使用者的選項                                                                                                                                                   |
| 詳情頁     | 移除 `breadcrumb-root`、`ink-meta` 兩個量過的 token                                                                                | 對應的東西（麵包屑、總銷量）決定不做。token 表只描述畫面上真的有的東西；數值保留在 `design-tokens.md` 的文字裡                                                                                                                                                                 |
| 共用元件   | embla 分兩次安裝（先拿掉、做到 `Carousel` 才加回來）                                                                               | `@nx/dependency-checks` 會擋下「宣告了卻沒用到」的依賴。要讓每個 commit 單獨為綠，依賴必須和第一次使用它的程式碼在同一個 commit                                                                                                                                                |
| 限時搶購   | `stock-left` 與 `grab-badge` 合成一個 `card-footer`                                                                                | 兩者各只有幾行，永遠一起出現在卡片底部同一列。拆成兩個檔案只會多一層要組合的東西                                                                                                                                                                                               |
| 限時搶購   | 標題列用文字、圖示與 token 重現，不是圖                                                                                            | 動工時查證：真站的整條標題列是一張圖，素材裡沒有。粉色底從目標截圖取樣，token 的說明寫明來源                                                                                                                                                                                   |
| 限時搶購   | `get-remaining` 的小時不進位成天                                                                                                   | 真站的倒數只有時：分：秒三格；超過 24 小時就顯示 25 以上，比多一格「天」接近原樣，也不會壞版                                                                                                                                                                                   |
| 限時搶購   | 多了兩個規格以外的情境：載入失敗、沒有商品 → 區塊不顯示                                                                            | 和推薦區同一個原則：空的標題列加倒數比沒有這個區塊更糟。失敗由 app 的 QueryCache 回報                                                                                                                                                                                          |
| 限時搶購   | 「切換下一頁」的 scenario 沒有在 feature 裡寫點擊測試                                                                              | jsdom 不排版，真的 embla 永遠只有一頁。feature 的 spec 驗的是它自己的責任（切成 10 / 10 / 9、順序不跳不重複）；點擊換頁是 `Carousel` 的責任，已在 `shared/ui` 對假的 embla 測過，並在瀏覽器驗了狀態                                                                            |
| 今日暢銷榜 | 不是 feature package，是 `product-rail` 的一筆設定；`home/feature-ranking`、`ranking` 型別、`getRanking()` / `useRanking` 都移除了 | 計畫在還沒看過這個區塊是什麼之前就替它開了 package。做到時它沒有自己的邏輯；真站的 CMS 也把它和 momo 店取當成同一種區塊。`architecture.md` §5 早就寫了「沒有邏輯的區塊不拆」，這次是照自己的規則執行                                                                           |
| 今日暢銷榜 | 版位資料多了 `background`（CSS 顏色）與標題的 `badge`                                                                              | 真站上這兩樣都是這個區塊實例的資料。`background` 不進 token：它是 CMS 隨檔期給的內容                                                                                                                                                                                           |
| 今日暢銷榜 | `ProductCard` 多了 `frame="bordered"`，momo 店取也改用它                                                                           | 橫式卡在真站是 1px `#d9d9d9`、沒有陰影；原本沿用的是直式卡（降價好貨）量到的樣式                                                                                                                                                                                               |
| 今日暢銷榜 | 頁面層的 spec 一寫就是綠的，改用變異測試確認它會紅                                                                                 | 既有的 block 本來就滿足規格，沒有「先紅」可言。把順序反過來、在卡片上印名次，兩種變異都讓它紅                                                                                                                                                                                  |
| 交付       | CI 在 main 上跑全部專案，不是計畫寫的 `nx affected`；另外多跑 `format:check`、`verify:boundaries`、`verify:fixtures`               | `affected` 的第一次執行什麼都沒跑（見 §4）。沒有 remote cache 的情況下，main 用全跑換確定性（約 2 分鐘），PR 才用 affected 換速度。兩個 verify 是因為 lint 的規則曾經靜默失效過                                                                                                |
| 交付       | CI 是最後一步才加的                                                                                                                | 計畫把它排在「可砍」。事後看是排錯了：lockfile 過期的假綠燈（§4）在第一天就會被 CI 抓到，而不是靠後來偶然建立的乾淨複本                                                                                                                                                        |

## 6. 環境備註

開發機是 2018 年的筆電（i7-8750H、16 GB RAM），專案位於 5400 轉的傳統硬碟，防毒即時掃描開啟。每個 Nx / Vitest 指令都很慢 —— 實測一個 0.5 秒的測試總耗時 34 秒（import 16 秒 + jsdom 環境 16 秒）。**與程式碼無關。**

**診斷被修正過一次，值得記下來。** Agent 起初把原因歸給傳統硬碟，並預期搬到 SSD 會快 5–10 倍。在 SSD 上建立測試複本後實測：安裝依賴確實快很多（50 秒 vs 3 分鐘），但跑測試沒有明顯變快。再量才發現 CPU 負載 100%、可用記憶體只剩 1.5 GB —— **真正的瓶頸是 CPU 與記憶體**（每個 Vitest 行程都要起一個 jsdom，Nx 預設同時跑 3 個 task，再加上 daemon 與 plugin worker），硬碟只是讓它更糟。教訓：先量再下結論；第一個看起來合理的原因不一定是主因。

決定：專案維持在原磁碟（基準）。另有一份放在 SSD 的測試複本，**只用來跑安裝與測試**：單向同步（基準 → 複本）、複本沒有 `.git` 所以無法從那裡 commit。它同時是一個沒有 `node_modules` 的乾淨環境，第一次使用就抓到 lockfile 過期的假綠燈（見 §4）。

因應方式：

```bash
NX_DAEMON=false NX_ISOLATE_PLUGINS=false pnpm nx run-many -t lint test typecheck --parallel=1
```

- `NX_ISOLATE_PLUGINS=false`：否則 plugin worker 子行程無法在 30 秒內連線而載入失敗。
- `--parallel=1`：否則 Vitest worker 啟動逾時，而且同時跑多個 jsdom 會把 CPU 與記憶體吃滿。
- 日常開發使用 Nx 快取；只有驗證關卡才加 `--skip-nx-cache`。
- 失敗的 Nx 指令會留下孤兒 node 行程（曾發現 7 個掛了 23–55 分鐘），它們會持續佔用資源讓後續指令更慢。指令異常結束後要檢查並清掉。

在資源充足的機器上不需要這些參數。

## 7. 協作效率評估

時間紀錄在專案初期就依 Human 的要求全部拿掉了，所以這一節用**數得出來的東西**評估，不用工時。

### 數字

| 項目             | 數量                              | 說明                                                                                                                         |
| ---------------- | --------------------------------- | ---------------------------------------------------------------------------------------------------------------------------- |
| Commit           | 約 80 個，全部帶 `Co-Authored-By` | feat 30、docs 24、refactor 8、chore 6、fix 4、build 3、ci 2、其他 2。docs 佔三成：每一步的紀錄、ADR 與規格都和程式碼一起進版 |
| 自動化測試       | 163 個，分布在 10 個專案          | 只打有邏輯的地方；純版面的 block 沒有單元測試                                                                                |
| Human 糾正 Agent | 12 次（§2）                       | 8 次關於方向、範圍與結構；3 次是「去對照真實網站」；1 次是程式碼註解的可讀性                                                 |
| Agent 出錯與事故 | 31 件（§4）                       | 見下方「誰抓到的」                                                                                                           |
| 與原計畫的偏離   | 47 項（§5）                       | 多數是實作時發現計畫憑印象寫錯，或計畫預留了沒有使用者的東西                                                                 |

### 31 件事故是誰抓到的

| 抓到的方式                                 | 件數 | 例子                                                                                                      |
| ------------------------------------------ | ---- | --------------------------------------------------------------------------------------------------------- |
| Agent 自己檢查產出、讀原始輸出、動手前先查 | 11   | tags 靜默失效、探針寫檔失敗、CI 的空綠燈、公開前逐張看截圖                                                |
| Human 提問或要求複查                       | 8    | 規格寫了沒驗證過的行為、layout 的三次改口、自己編的 requirement；以及一件操作層級的：驗證指令掛了 20 分鐘 |
| 設計成「會失敗」的工具與乾淨環境           | 7    | 沒有 `node_modules` 的複本抓到 lockfile 過期、匯入工具對沒交代的檔案 exit 1、比對 build 產物雜湊          |
| 自動化測試                                 | 5    | 用真的 embla 掛載一次、同名的巢狀 landmark、連點造成的重複請求                                            |

兩個觀察：

- **Human 抓到的 8 件裡，7 件是「方向」或「事實」層級的錯**（規格、架構的說法、需求是不是真的存在），沒有一件是某一行程式碼寫錯。第 8 件是例外，而且不光彩：Agent 的一個背景指令掛了 20 分鐘，是 Human 等得不耐煩才問出來的。工具與測試抓到的 12 件則全部是實作層級。兩者幾乎不重疊 —— Human 的 review 無法用更多測試取代，反過來也是。
- **同一類錯誤重複出現**：「檢查回報成功，但其實什麼都沒檢查」出現了 4 次（tags、`dependency-checks`、探針、CI）。每次都把教訓寫進紀錄，下一次還是換個形式再犯。真正有效的不是紀錄，而是後來養成的兩個習慣：結果快得或整齊得不合理就去讀原始輸出；負向驗證要看到「因為正確的理由而失敗」。

### 哪裡快、哪裡慢

**Agent 明顯加速的工作**：

- 機械性的大範圍修改：`libs/` → `packages/`（含依賴宣告下放）、`shell` → `layout` 改名、118 個檔案的註解改寫。每一項都逐 commit 保持綠燈，人工做會是以天計的工作。
- 到真實網站用 DOM 量測數值（顏色、字級、間距、區塊結構），再寫成 token 與對照文件。
- 規格 → scenario → 測試 → 實作的 TDD 迴圈；scenario 寫得夠具體時，測試幾乎是逐條翻譯。
- 文件：ADR、規格、每一步的紀錄。

**最貴的是重工，而重工幾乎都來自「沒先查證就提案」**：

| 重工                                                                         | 起因                                                    |
| ---------------------------------------------------------------------------- | ------------------------------------------------------- |
| layout 改名一次、搬家一次、再查證一次（糾正 6、7、8）                        | 沒有先查 Nx 的慣例；先有結論再找理由                    |
| `libs/` → `packages/` 與依賴宣告下放（糾正 9）                               | 沒有對照 pnpm 與 Nx 對 package 的規範                   |
| 設計 token 重量一次（糾正 5）                                                | 顏色一開始是看截圖估的                                  |
| 一條規格、一個 package（名次徽章、`feature-ranking`）寫了又刪（糾正 10、12） | 需求來自對「暢銷榜」三個字的聯想，沒有對照真站          |
| 868 行註解改寫（糾正 11）                                                    | 把設計理由寫進程式碼，混淆了註解、commit message 與 ADR |

12 次糾正裡有 3 次的內容就是「去看真實網站」。**如果重來一次，最大的效率改善不是寫得更快，而是把「對照真實系統」與「查慣例」放在每個提案之前。**

另一個慢的來源與 Agent 無關：開發機上每個 Nx 指令 30–60 秒、完整驗證關卡 5–8 分鐘（§6）。它讓「每個 commit 都單獨驗證為綠」這條紀律變得很貴，但沒有因此放掉。

### 這套協作方式裡，實際起作用的是什麼

1. **設計文件是 source of truth，先於實作。** 前 4 次糾正發生在還沒寫任何程式之前，改的只是文件；之後的 8 次都要動到已經寫好的東西，每一次都貴得多。越早把設計攤開給 Human 看，糾正越便宜。
2. **一次一步、每步有 review 關卡。** Agent 不偷做下一步，所以方向錯的時候最多賠一步。
3. **把教訓變成規則，而不只是紀錄。** `CLAUDE.md`「做事的方式」裡的多數條目都對應到一次事故：探針的輸出要出現規則名稱才算被擋、只斷言「不存在」的測試要同時斷言一個「存在」、聲稱「會被工具擋下」的行為要放過違規樣本、註解的寫法。Agent 每次工作階段都會讀到它。
4. **驗證要能失敗。** `verify:boundaries` 有自我測試、匯入工具對沒交代的檔案 exit 1、乾淨環境的 frozen install、綠得太早的測試用變異測試補驗。
5. **沒驗到的照實寫。** 每一步的回報都有「沒有驗到的」一欄（瀏覽器面板在背景時的截圖、動畫），之後補驗到了再回頭更新，而不是預設它沒問題。

### 下次會改的做法

- **CI 第一天就加**，不排在最後。
- **每個區塊動工前先讀真站的 DOM**，不只看截圖。Step 12 才這樣做，結果除了確認切法，還順手抓到 Step 8 留下的兩個樣式錯誤。
- **package 等有邏輯了再開。** 計畫預先開了 `feature-ranking`，做到時發現它沒有邏輯，整個刪掉。
- **提案涉及業界慣例時先查證再開口**，並把改動成本與還沒做的工作算進去。
